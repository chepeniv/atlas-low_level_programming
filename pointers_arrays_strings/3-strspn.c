#include "main.h"

/**
 * _strspn - find the length of a prefix substring wherein a set of given
 * characters occur
 * @string: the string to search through
 * @search_set: the string containing the char to search for
 *
 * Return: an unsigned int denoting the length of the substring containg at 
 * least one instance of each element of search_set
 */
unsigned int _strspn(char *string, char *search_set);
{
	int set_i = 0;
	int str_i = 0;
	int size = 0;

	while (search_set[set_i] != '\0')
	{
		while (string[str_i] != '\0')
			if 
		int str_i = 0;
		index++;
	}

	if (string[index] == csearch)
		return (&string[index]);
	else
		return (NULL);
}
