#include "main.h"
#include <stdio.h>

/* testing task's sourcecode locally */
int main(void) 
{
	//more_numbers();
	print_square(-10);
	print_square(0);
	print_square(2);
	print_square(4);
	print_square(8);
	print_square(16);
	//print_diagonal(32);
	return (0);
}
