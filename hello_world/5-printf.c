#include <stdio.h>

/**
 * main - prints out a predefined string to stdout
 *
 * Return: returns 0 upon successful execution
 */
int main(void)
{
	const char s[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", s);
	return (0);
}
