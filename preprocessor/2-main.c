#include "main.h"
#include <stdio.h>

/**
 * main - a functions that printsout the filename that it was
 * compiled from
 *
 * Return: 0
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
