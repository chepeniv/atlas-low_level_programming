#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_list -
 * @h: a pointer to the first item in a linked list
 *
 * Return: the number of nodes
 */
size_t print_list(const list_t *h)
{
	/* printf is permited */
	/* return number of nodes */
	/* print "[0] (nil)" if str == NULL */
}
